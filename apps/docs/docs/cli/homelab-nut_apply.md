---
title: homelab-nut apply
slug: /cli/homelab-nut_apply
---

## homelab-nut apply

Plan + execute changes on each host over SSH

### Synopsis

Loads the inventory, plans what would change on each host, prompts
for confirmation (unless --auto-approve), then executes Apply for each
role with a non-empty diff. Streams each role's output prefixed with
[host/role] so concurrent fleet output stays attributable.

```
homelab-nut apply [flags]
```

### Options

```
  -y, --auto-approve      skip the y/N confirmation prompt before applying
      --concurrency int   max hosts to apply against in parallel (0 = unlimited)
  -h, --help              help for apply
  -o, --output string     output format: text (human) | json (machine-readable for AI/scripts) (default "text")
```

### Options inherited from parent commands

```
  -i, --inventory string   path to the inventory YAML file (default "homelab-nut.yaml")
```

### SEE ALSO

* [homelab-nut](./homelab-nut)	 - Modern CLI + TUI for Network UPS Tools (NUT)

###### Auto generated by spf13/cobra on 19-Jun-2026
